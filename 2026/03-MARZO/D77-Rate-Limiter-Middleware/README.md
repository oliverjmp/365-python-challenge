# Módulo D77: Rate Limiter Middleware (`Slowapi + Python`)

Implementación de un middleware de control de tráfico y limitación de tasa (*rate limiting*) para prevenir abusos en APIs con FastAPI y Slowapi.

## Requisitos e Instalación
```powershell
pip install -r requirements.txt