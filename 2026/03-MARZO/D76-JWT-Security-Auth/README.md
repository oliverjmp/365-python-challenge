# Módulo D76: JWT Security Auth (`FastAPI + JWT`)

Implementación de autenticación y autorización segura mediante JSON Web Tokens (JWT) para microservicios analíticos desarrollados con FastAPI.

## Requisitos e Instalación
```powershell
pip install -r requirements.txt