# Módulo D73: CLI Command Center (`Typer / Click`)

Interfaz de línea de comandos profesional construida con Typer para la orquestación de la ejecución de pipelines de datos.

## Uso del CLI

### Ejecutar simulación del pipeline:
```powershell
python src/cli.py --name mi-pipeline --dry-run