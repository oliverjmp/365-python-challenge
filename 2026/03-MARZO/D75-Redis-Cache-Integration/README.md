# Módulo D75: Redis Cache Integration (`Redis + Python`)

Integración de una capa de caché de alto rendimiento en Python utilizando el cliente de Redis.

## Requisitos e Instalación
```powershell
pip install -r requirements.txt